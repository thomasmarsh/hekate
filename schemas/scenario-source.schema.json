{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ScenarioSourceV2",
  "description": "A version-2 scenario document.\n\nVersion 2 replaces the version-1 `profiles`/`pedestrian_profiles`,\n`population`, `demand`, and `pedestrian_demand` fields with `mode_templates`\nand a mode-tagged `demand`, and makes each movement's `direction` explicit.\nThe Increment 1 additive arrays `facilities`, `facility_connectors`, and\n`permissions` extend it in place. Every other version-1 field is carried\nforward unchanged.",
  "type": "object",
  "properties": {
    "boundaries": {
      "description": "Closed polygons marking the non-traversable world limits.",
      "type": "array",
      "default": [],
      "items": {
        "$ref": "#/$defs/PolygonSource"
      }
    },
    "conflict_regions": {
      "description": "Authored conflict regions shared by pairs of movements.",
      "type": "array",
      "default": [],
      "items": {
        "$ref": "#/$defs/ConflictRegionSource"
      }
    },
    "coordinate_system": {
      "description": "Human-facing names for the two world axes.",
      "$ref": "#/$defs/CoordinateSystem"
    },
    "crossings": {
      "description": "Pedestrian crossings over one or more movements.",
      "type": "array",
      "default": [],
      "items": {
        "$ref": "#/$defs/CrossingSource"
      }
    },
    "demand": {
      "description": "Mode-tagged demand sources for every mode.",
      "type": "array",
      "default": [],
      "items": {
        "$ref": "#/$defs/DemandSourceV2"
      }
    },
    "facilities": {
      "description": "Continuous-width facilities with usable width, nominal direction, mode\naccess, lateral-use policy, and speed policy. Additive Increment 1\narray; omitted or empty means no facilities.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/FacilitySource"
      }
    },
    "facility_connectors": {
      "description": "Directed connectors joining two facility traversals. Additive\nIncrement 1 array.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/FacilityConnectorSource"
      }
    },
    "id": {
      "description": "Stable scenario identifier recorded in run provenance.",
      "type": "string"
    },
    "mode_templates": {
      "description": "Named, validated mode bundles; demand references them by id.",
      "type": "array",
      "default": [],
      "items": {
        "$ref": "#/$defs/ModeTemplateSource"
      }
    },
    "movements": {
      "description": "Movement connectors with an explicit nominal direction.",
      "type": "array",
      "default": [],
      "items": {
        "$ref": "#/$defs/MovementSourceV2"
      }
    },
    "paths": {
      "description": "Guide paths agents travel along.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/PathSource"
      }
    },
    "pedestrian_routes": {
      "description": "Pedestrian routes from one portal to another along a guide path.",
      "type": "array",
      "default": [],
      "items": {
        "$ref": "#/$defs/PedestrianRouteSource"
      }
    },
    "permissions": {
      "description": "Authored permission and obligation statements. Additive Increment 1\narray; the shape is fixed in full, while only `nominal_direction`\nstatements are populated in Increment 1.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/PermissionSource"
      }
    },
    "portals": {
      "description": "Entry and exit points attached to path ends.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/PortalSource"
      }
    },
    "regions": {
      "description": "Closed polygons marking traversable areas other than guide paths.",
      "type": "array",
      "default": [],
      "items": {
        "$ref": "#/$defs/PolygonSource"
      }
    },
    "rules": {
      "description": "Right-of-way or control rules attached to movements.",
      "type": "array",
      "default": [],
      "items": {
        "$ref": "#/$defs/RuleSource"
      }
    },
    "schema_version": {
      "description": "Schema version the document was written against.",
      "type": "integer",
      "format": "uint32",
      "minimum": 0
    },
    "signals": {
      "description": "Fixed-time signal controllers with phased signal heads.",
      "type": "array",
      "default": [],
      "items": {
        "$ref": "#/$defs/SignalSource"
      }
    },
    "waiting_areas": {
      "description": "Named waiting areas where pedestrians stage before crossing.",
      "type": "array",
      "default": [],
      "items": {
        "$ref": "#/$defs/WaitingAreaSource"
      }
    }
  },
  "additionalProperties": false,
  "required": [
    "schema_version",
    "id",
    "coordinate_system",
    "paths",
    "portals"
  ],
  "$defs": {
    "AccessSource": {
      "description": "Facility access of a version-2 mode template.",
      "type": "object",
      "properties": {
        "facility_kinds": {
          "description": "Traversable object kinds the mode may use.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/FacilityKind"
          }
        },
        "nominal_direction": {
          "description": "Direction the mode may travel on its facilities.\n\nOmitted means `either`, the value Increment 0 fixed for the compiled\naccess direction because nothing authored it. Additive Increment 1\nfield: an Increment 0 template omits it and keeps that meaning.",
          "anyOf": [
            {
              "$ref": "#/$defs/FacilityDirection"
            },
            {
              "type": "null"
            }
          ]
        },
        "speed_policy": {
          "description": "Speed policy the mode travels under.\n\nOmitted means no enforced limit, the value Increment 0 fixed for the\ncompiled access. Additive Increment 1 field.",
          "anyOf": [
            {
              "$ref": "#/$defs/SpeedPolicySource"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "facility_kinds"
      ]
    },
    "ConflictRegionSource": {
      "description": "An authored conflict region shared by two movements.\n\nThe geometry lets a renderer or metric show where two movement envelopes\ncross; the movement pair is what a signal check uses to reject conflicting\ngreens. A scenario may author it explicitly or, later, accept a derived one.",
      "type": "object",
      "properties": {
        "id": {
          "description": "Stable identifier, unique across all authored objects.",
          "type": "string"
        },
        "movements": {
          "description": "Exactly two movements whose envelopes conflict in this region.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "points": {
          "description": "Ordered ring vertices in metres.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/PointSource"
          }
        }
      },
      "additionalProperties": false,
      "required": [
        "id",
        "points",
        "movements"
      ]
    },
    "CoordinateSystem": {
      "description": "Human-facing names for the world coordinate axes.",
      "type": "object",
      "properties": {
        "x": {
          "description": "Name of the positive x axis, for example `east_m`.",
          "type": "string"
        },
        "y": {
          "description": "Name of the positive y axis, for example `north_m`.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "x",
        "y"
      ]
    },
    "CrossingSource": {
      "description": "A pedestrian crossing occupying a traversable region over some movements.",
      "type": "object",
      "properties": {
        "id": {
          "description": "Stable identifier, unique across all authored objects.",
          "type": "string"
        },
        "movements": {
          "description": "Movements the crossing crosses.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "pedestrian_signal": {
          "description": "Fixed-time pedestrian signal rule for this crossing.\n\nOmitted (the default) is an uncontrolled crossing: pedestrians cross it\nfreely, subject to ordinary interaction. Additive schema version 1\nfield, mirroring the `stop_line_m`/`compliance` precedent.",
          "anyOf": [
            {
              "$ref": "#/$defs/PedestrianSignalSource"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "region": {
          "description": "Traversable region the crossing occupies.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "id",
        "region",
        "movements"
      ]
    },
    "DemandChoiceSource": {
      "description": "Choice of movements or routes for a version-2 rate demand spawn.\n\nVehicle modes choose among movements; pedestrian modes choose among\npedestrian routes.",
      "oneOf": [
        {
          "description": "Movements a generated vehicle may follow, with relative weights.",
          "type": "object",
          "properties": {
            "movements": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/RouteShareSource"
              }
            }
          },
          "additionalProperties": false,
          "required": [
            "movements"
          ]
        },
        {
          "description": "Pedestrian routes a generated pedestrian may follow, with weights.",
          "type": "object",
          "properties": {
            "routes": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/PedestrianRouteShareSource"
              }
            }
          },
          "additionalProperties": false,
          "required": [
            "routes"
          ]
        }
      ]
    },
    "DemandPopulationSpawnSource": {
      "description": "A fixed initial-population version-2 demand spawn, placed at `t = 0`.",
      "type": "object",
      "properties": {
        "count": {
          "description": "Number of agents in the population.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "path": {
          "description": "Guide path the population is placed on.",
          "type": "string"
        },
        "spacing_m": {
          "description": "Longitudinal gap between adjacent agents in metres.",
          "type": "number",
          "format": "double"
        },
        "speed_mps": {
          "description": "Constant speed in metres per second.",
          "type": "number",
          "format": "double"
        }
      },
      "additionalProperties": false,
      "required": [
        "path",
        "count",
        "speed_mps",
        "spacing_m"
      ]
    },
    "DemandRateSpawnSource": {
      "description": "A rate-driven version-2 demand spawn.",
      "type": "object",
      "properties": {
        "choice": {
          "description": "Movements or routes a generated agent may follow.",
          "$ref": "#/$defs/DemandChoiceSource"
        },
        "interval_s": {
          "description": "Interval over which the rate applies.",
          "$ref": "#/$defs/TimeIntervalSource"
        },
        "portal": {
          "description": "Entry portal where generated agents enter the world.",
          "type": "string"
        },
        "rate_per_hour": {
          "description": "Mean arrival rate in agents per hour.",
          "type": "number",
          "format": "double"
        }
      },
      "additionalProperties": false,
      "required": [
        "portal",
        "rate_per_hour",
        "interval_s",
        "choice"
      ]
    },
    "DemandSourceV2": {
      "description": "One mode-tagged version-2 demand source.",
      "type": "object",
      "properties": {
        "id": {
          "description": "Stable identifier, unique across all authored objects.",
          "type": "string"
        },
        "mode": {
          "description": "Mode-template id this demand produces.",
          "type": "string"
        },
        "spawn": {
          "description": "How agents are placed.",
          "$ref": "#/$defs/DemandSpawnSource"
        }
      },
      "additionalProperties": false,
      "required": [
        "id",
        "mode",
        "spawn"
      ]
    },
    "DemandSpawnSource": {
      "description": "How a version-2 demand source places agents.",
      "oneOf": [
        {
          "description": "Arrivals at a portal over a time interval.",
          "type": "object",
          "properties": {
            "rate": {
              "$ref": "#/$defs/DemandRateSpawnSource"
            }
          },
          "additionalProperties": false,
          "required": [
            "rate"
          ]
        },
        {
          "description": "A fixed population placed at `t = 0`.",
          "type": "object",
          "properties": {
            "population": {
              "$ref": "#/$defs/DemandPopulationSpawnSource"
            }
          },
          "additionalProperties": false,
          "required": [
            "population"
          ]
        }
      ]
    },
    "FacilityAccessSource": {
      "description": "Mode-template access of a version-2 facility.",
      "type": "object",
      "properties": {
        "modes": {
          "description": "Mode-template ids permitted to use the facility; non-empty.",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false,
      "required": [
        "modes"
      ]
    },
    "FacilityConnectorEndSource": {
      "description": "One end of a version-2 facility connector.",
      "type": "object",
      "properties": {
        "direction": {
          "description": "Traversal direction along that facility: `forward` or `reverse`.",
          "$ref": "#/$defs/MovementDirection"
        },
        "facility": {
          "description": "Facility the connector joins.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "facility",
        "direction"
      ]
    },
    "FacilityConnectorSource": {
      "description": "A version-2 directed connector joining two facility traversals.",
      "type": "object",
      "properties": {
        "from": {
          "description": "Facility and traversal direction the connector leaves.",
          "$ref": "#/$defs/FacilityConnectorEndSource"
        },
        "id": {
          "description": "Stable connector id, unique across all authored objects.",
          "type": "string"
        },
        "to": {
          "description": "Facility and traversal direction the connector enters.",
          "$ref": "#/$defs/FacilityConnectorEndSource"
        }
      },
      "additionalProperties": false,
      "required": [
        "id",
        "from",
        "to"
      ]
    },
    "FacilityDirection": {
      "description": "Direction a mode may travel along a facility reference path, relative to\nthe authored vertex order of that path.",
      "oneOf": [
        {
          "description": "Only the authored forward direction.",
          "type": "string",
          "const": "forward"
        },
        {
          "description": "Only the reverse of the authored direction.",
          "type": "string",
          "const": "reverse"
        },
        {
          "description": "Either direction.",
          "type": "string",
          "const": "either"
        }
      ]
    },
    "FacilityKind": {
      "description": "Traversable object kind a version-2 mode may use.",
      "oneOf": [
        {
          "description": "A guide path.",
          "type": "string",
          "const": "path"
        },
        {
          "description": "A pedestrian crossing.",
          "type": "string",
          "const": "crossing"
        },
        {
          "description": "A pedestrian waiting area.",
          "type": "string",
          "const": "waiting_area"
        },
        {
          "description": "A continuous-width facility: a region with an optional reference path.\nAdditive Increment 1 kind, so an Increment 0 template keeps its meaning.",
          "type": "string",
          "const": "facility"
        }
      ]
    },
    "FacilitySource": {
      "description": "One version-2 continuous-width facility: a traversable region plus an\noptional reference path, usable width, nominal direction, mode access,\nlateral-use policy, and speed policy.",
      "type": "object",
      "properties": {
        "access": {
          "description": "Mode templates permitted to use the facility.",
          "$ref": "#/$defs/FacilityAccessSource"
        },
        "id": {
          "description": "Stable facility id, unique across all authored objects.",
          "type": "string"
        },
        "lateral_use": {
          "description": "Whether the usable lateral interval is shared or centered.",
          "$ref": "#/$defs/LateralUse"
        },
        "nominal_direction": {
          "description": "Nominal direction relative to the authored vertex order of\n`reference_path`.",
          "$ref": "#/$defs/FacilityDirection"
        },
        "reference_path": {
          "description": "Guide path giving the facility its `(s, d)` frame; omitted means the\nfacility exposes region geometry only.",
          "type": [
            "string",
            "null"
          ]
        },
        "region": {
          "description": "Region the facility occupies.",
          "type": "string"
        },
        "speed_policy": {
          "description": "Speed policy on the facility; a missing field is a parse error.",
          "$ref": "#/$defs/SpeedPolicySource"
        },
        "width_m": {
          "description": "Usable traversable width in metres, measured across the reference path.",
          "type": "number",
          "format": "double"
        }
      },
      "additionalProperties": false,
      "required": [
        "id",
        "region",
        "width_m",
        "nominal_direction",
        "access",
        "lateral_use",
        "speed_policy"
      ]
    },
    "LateralUse": {
      "description": "Whether a facility's usable lateral interval is one shared space or a\ncentered lane.",
      "oneOf": [
        {
          "description": "Agents may occupy any position within the usable lateral interval.",
          "type": "string",
          "const": "shared"
        },
        {
          "description": "Agents hold the reference centerline.",
          "type": "string",
          "const": "centered"
        }
      ]
    },
    "ModeBodySource": {
      "description": "Body geometry distributions of one version-2 mode template.",
      "oneOf": [
        {
          "description": "A rectangular body; dimensions are ranges in metres.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "box"
            },
            "length_m": {
              "description": "Body length range in metres.",
              "$ref": "#/$defs/ProfileRangeSource"
            },
            "width_m": {
              "description": "Body width range in metres.",
              "$ref": "#/$defs/ProfileRangeSource"
            }
          },
          "required": [
            "kind",
            "length_m",
            "width_m"
          ]
        },
        {
          "description": "A circular body; the radius is a range in metres.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "circle"
            },
            "radius_m": {
              "description": "Body radius range in metres.",
              "$ref": "#/$defs/ProfileRangeSource"
            }
          },
          "required": [
            "kind",
            "radius_m"
          ]
        },
        {
          "description": "A capsule body: a segment of `length_m` with a constant `radius_m`.\n\nAdditive Increment 1 kind for the narrow wheeled family; it compiles to\nthe existing `AgentBody::Capsule` and the `wheeled_capsule` family.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "capsule"
            },
            "length_m": {
              "description": "Body length range in metres.",
              "$ref": "#/$defs/ProfileRangeSource"
            },
            "radius_m": {
              "description": "Body radius range in metres.",
              "$ref": "#/$defs/ProfileRangeSource"
            }
          },
          "required": [
            "kind",
            "length_m",
            "radius_m"
          ]
        }
      ]
    },
    "ModeTemplateSource": {
      "description": "One version-2 mode template: a named, validated bundle of body, motion,\ntactical capability, access, occupancy, and profile distributions.",
      "type": "object",
      "properties": {
        "access": {
          "description": "Traversable object kinds the mode may use.",
          "$ref": "#/$defs/AccessSource"
        },
        "body": {
          "description": "Body geometry distributions, tagged by `kind`.",
          "$ref": "#/$defs/ModeBodySource"
        },
        "id": {
          "description": "Stable template id, unique across authored objects.",
          "type": "string"
        },
        "motion": {
          "description": "Motion family the template uses.",
          "$ref": "#/$defs/MotionKind"
        },
        "occupancy": {
          "description": "Occupancy the template carries.",
          "$ref": "#/$defs/OccupancyKind"
        },
        "profiles": {
          "description": "Profile distributions keyed by parameter name.",
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/ProfileRangeSource"
          }
        },
        "tactics": {
          "description": "Tactical capabilities the template supports.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/TacticKind"
          }
        }
      },
      "additionalProperties": false,
      "required": [
        "id",
        "body",
        "motion",
        "tactics",
        "access",
        "occupancy",
        "profiles"
      ]
    },
    "MotionKind": {
      "description": "Motion family a version-2 mode template uses.",
      "oneOf": [
        {
          "description": "Walking on the Phase 1 pedestrian geometry.",
          "type": "string",
          "const": "holonomic_walking"
        },
        {
          "description": "A single wheeled body following a reference path.",
          "type": "string",
          "const": "single_body_wheeled"
        }
      ]
    },
    "MovementDirection": {
      "description": "Nominal traversal direction of a version-2 movement along its reference path.",
      "oneOf": [
        {
          "description": "The movement follows the authored vertex order of its path.",
          "type": "string",
          "const": "forward"
        },
        {
          "description": "The movement follows the reverse of the authored vertex order.",
          "type": "string",
          "const": "reverse"
        }
      ]
    },
    "MovementSourceV2": {
      "description": "A version-2 movement connector with an explicit nominal direction.\n\nEvery version-1 movement field is carried forward unchanged; version 2 adds\n`direction` so the normalized document is self-describing instead of leaving\nthe direction implicit in the `from`/`to` portal order.",
      "type": "object",
      "properties": {
        "direction": {
          "description": "Nominal traversal direction of `path`.",
          "$ref": "#/$defs/MovementDirection"
        },
        "from": {
          "description": "Portal where the movement begins.",
          "type": "string"
        },
        "id": {
          "description": "Stable identifier, unique across all authored objects.",
          "type": "string"
        },
        "path": {
          "description": "Guide path the movement follows.",
          "type": "string"
        },
        "priority": {
          "description": "Right-of-way rank; a lower value is honored before a higher one.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "stop_line_m": {
          "description": "Stop-line arc length in metres from the movement entry.",
          "type": "number",
          "format": "double",
          "default": 0.0
        },
        "to": {
          "description": "Portal where the movement ends.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "id",
        "from",
        "to",
        "path",
        "priority",
        "direction"
      ]
    },
    "OccupancyKind": {
      "description": "Occupancy of a version-2 mode template.",
      "oneOf": [
        {
          "description": "One operator, no passengers.",
          "type": "string",
          "const": "operator_only"
        }
      ]
    },
    "PathEnd": {
      "description": "Which end of a path a portal is attached to.",
      "oneOf": [
        {
          "description": "The first vertex of the path.",
          "type": "string",
          "const": "start"
        },
        {
          "description": "The last vertex of the path.",
          "type": "string",
          "const": "end"
        }
      ]
    },
    "PathSource": {
      "description": "One authored guide path as an ordered polyline.",
      "type": "object",
      "properties": {
        "id": {
          "description": "Stable path identifier, unique across all authored objects.",
          "type": "string"
        },
        "points": {
          "description": "Ordered vertices of the guide path.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/PointSource"
          }
        }
      },
      "additionalProperties": false,
      "required": [
        "id",
        "points"
      ]
    },
    "PedestrianRouteShareSource": {
      "description": "One pedestrian route's relative share of a pedestrian demand source's\narrivals.",
      "type": "object",
      "properties": {
        "route": {
          "description": "Pedestrian route a generated pedestrian follows.",
          "type": "string"
        },
        "weight": {
          "description": "Relative weight; a larger weight is chosen proportionally more often.",
          "type": "number",
          "format": "double"
        }
      },
      "additionalProperties": false,
      "required": [
        "route",
        "weight"
      ]
    },
    "PedestrianRouteSource": {
      "description": "A pedestrian route from one portal to another along a guide path.\n\nA route is the pedestrian routing primitive, mirroring [`MovementSource`]\nfor the vehicle mode. It reuses the same path geometry and portals, and\nadditionally names the crossings and waiting areas it passes through in\ntravel order, so signal compliance and staging can be attached per route\nwithout naming a scenario kind.",
      "type": "object",
      "properties": {
        "crossings": {
          "description": "Crossings the route traverses, in travel order.",
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        },
        "from": {
          "description": "Portal where the route begins.",
          "type": "string"
        },
        "id": {
          "description": "Stable identifier, unique across all authored objects.",
          "type": "string"
        },
        "path": {
          "description": "Guide path the route follows.",
          "type": "string"
        },
        "to": {
          "description": "Portal where the route ends.",
          "type": "string"
        },
        "waiting_areas": {
          "description": "Waiting areas the route stages at, in travel order.",
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false,
      "required": [
        "id",
        "from",
        "to",
        "path"
      ]
    },
    "PedestrianSignalPhaseSource": {
      "description": "One fixed-time pedestrian signal phase.",
      "type": "object",
      "properties": {
        "duration_s": {
          "description": "Phase duration in seconds.",
          "type": "number",
          "format": "double"
        },
        "walk": {
          "description": "Whether pedestrians may cross during this phase.",
          "type": "boolean"
        }
      },
      "additionalProperties": false,
      "required": [
        "duration_s",
        "walk"
      ]
    },
    "PedestrianSignalSource": {
      "description": "A fixed-time pedestrian signal rule embedded in one crossing.\n\nIt is the pedestrian analogue of [`SignalSource`] but its controlled object\nis the owning crossing rather than a movement, so a pedestrian rule needs no\nvehicle signal. Its phases alternate a walk interval, during which crossing\nis permitted, and a don't-walk interval, during which the signal forbids\ncrossing; the phases are contiguous and cover the cycle.",
      "type": "object",
      "properties": {
        "phases": {
          "description": "Phases in cycle order.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/PedestrianSignalPhaseSource"
          }
        }
      },
      "additionalProperties": false,
      "required": [
        "phases"
      ]
    },
    "PermissionEffect": {
      "description": "Whether a permission statement permits, prohibits, or obligates.",
      "oneOf": [
        {
          "description": "The holder may do the statement's action.",
          "type": "string",
          "const": "permit"
        },
        {
          "description": "The holder must not do it.",
          "type": "string",
          "const": "prohibit"
        },
        {
          "description": "The holder must do it.",
          "type": "string",
          "const": "obligate"
        }
      ]
    },
    "PermissionKind": {
      "description": "What an authored permission or obligation statement is about.",
      "oneOf": [
        {
          "description": "Travel against a facility's or movement's nominal direction.",
          "type": "string",
          "const": "nominal_direction"
        },
        {
          "description": "Permitted lateral position or lane use on a facility.",
          "type": "string",
          "const": "lane_use"
        },
        {
          "description": "Overtaking or passing on a facility.",
          "type": "string",
          "const": "overtake"
        },
        {
          "description": "Permission or obligation at a crossing.",
          "type": "string",
          "const": "crossing"
        },
        {
          "description": "Stop-service obligation and priority.",
          "type": "string",
          "const": "stop_service"
        }
      ]
    },
    "PermissionSource": {
      "description": "One authored permission or obligation statement.",
      "type": "object",
      "properties": {
        "effect": {
          "description": "Whether the statement permits, prohibits, or obligates.",
          "$ref": "#/$defs/PermissionEffect"
        },
        "holder": {
          "description": "Mode-template id the statement binds.",
          "type": "string"
        },
        "id": {
          "description": "Stable id, unique across all authored objects.",
          "type": "string"
        },
        "kind": {
          "description": "Kind of statement; it fixes the target object kind.",
          "$ref": "#/$defs/PermissionKind"
        },
        "target": {
          "description": "Id of the object the statement is about.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "id",
        "kind",
        "holder",
        "target",
        "effect"
      ]
    },
    "PointSource": {
      "description": "A world-space vertex in metres.",
      "type": "object",
      "properties": {
        "x": {
          "description": "East/north coordinate in metres.",
          "type": "number",
          "format": "double"
        },
        "y": {
          "description": "East/north coordinate in metres.",
          "type": "number",
          "format": "double"
        }
      },
      "additionalProperties": false,
      "required": [
        "x",
        "y"
      ]
    },
    "PolygonSource": {
      "description": "A closed polygon in metres.\n\nThe ring closes implicitly from the last vertex back to the first, so the\nauthored vertex list must not repeat the first point at the end.",
      "type": "object",
      "properties": {
        "id": {
          "description": "Stable identifier, unique across all authored objects.",
          "type": "string"
        },
        "points": {
          "description": "Ordered ring vertices in metres.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/PointSource"
          }
        }
      },
      "additionalProperties": false,
      "required": [
        "id",
        "points"
      ]
    },
    "PortalSource": {
      "description": "An entry or exit point attached to one end of a path.",
      "type": "object",
      "properties": {
        "end": {
          "description": "Which end of the path the portal marks.",
          "$ref": "#/$defs/PathEnd"
        },
        "id": {
          "description": "Stable portal identifier, unique across all authored objects.",
          "type": "string"
        },
        "path": {
          "description": "Identifier of the path this portal belongs to.",
          "type": "string"
        },
        "width_m": {
          "description": "Traversable width of the portal in metres.",
          "type": "number",
          "format": "double"
        }
      },
      "additionalProperties": false,
      "required": [
        "id",
        "path",
        "end",
        "width_m"
      ]
    },
    "ProfileRangeSource": {
      "description": "An inclusive uniform distribution for one profile parameter.\n\nA range with `min == max` is a constant. The owning field name carries the\nphysical unit, for example `speed_mps` or `time_gap_s`.",
      "type": "object",
      "properties": {
        "max": {
          "description": "Upper bound of the distribution.",
          "type": "number",
          "format": "double"
        },
        "min": {
          "description": "Lower bound of the distribution.",
          "type": "number",
          "format": "double"
        }
      },
      "additionalProperties": false,
      "required": [
        "min",
        "max"
      ]
    },
    "RouteShareSource": {
      "description": "One movement's relative share of a demand source's arrivals.",
      "type": "object",
      "properties": {
        "movement": {
          "description": "Movement a generated vehicle follows.",
          "type": "string"
        },
        "weight": {
          "description": "Relative weight; a larger weight is chosen proportionally more often.",
          "type": "number",
          "format": "double"
        }
      },
      "additionalProperties": false,
      "required": [
        "movement",
        "weight"
      ]
    },
    "RuleKind": {
      "description": "How a movement's right of way is controlled.",
      "oneOf": [
        {
          "description": "No control; the movement proceeds subject to ordinary interaction.",
          "type": "string",
          "const": "free"
        },
        {
          "description": "The movement must yield to the conflicting movements.",
          "type": "string",
          "const": "yield"
        },
        {
          "description": "The movement must stop before proceeding.",
          "type": "string",
          "const": "stop"
        },
        {
          "description": "A signal controller governs the movement.",
          "type": "string",
          "const": "signal"
        }
      ]
    },
    "RuleSource": {
      "description": "A control rule attached to one movement.",
      "type": "object",
      "properties": {
        "id": {
          "description": "Stable identifier, unique across all authored objects.",
          "type": "string"
        },
        "kind": {
          "description": "Kind of control the rule applies.",
          "$ref": "#/$defs/RuleKind"
        },
        "movement": {
          "description": "Movement this rule governs.",
          "type": "string"
        },
        "signal": {
          "description": "Signal controller, present exactly when `kind` is `signal`.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "additionalProperties": false,
      "required": [
        "id",
        "movement",
        "kind"
      ]
    },
    "SignalColor": {
      "description": "Display color of one signal head during one phase.",
      "oneOf": [
        {
          "description": "The controlled movement must stop.",
          "type": "string",
          "const": "red"
        },
        {
          "description": "The controlled movement should prepare to stop.",
          "type": "string",
          "const": "yellow"
        },
        {
          "description": "The controlled movement may proceed.",
          "type": "string",
          "const": "green"
        }
      ]
    },
    "SignalHeadSource": {
      "description": "One signal head controlling a movement.",
      "type": "object",
      "properties": {
        "id": {
          "description": "Identifier unique within the owning signal.",
          "type": "string"
        },
        "movement": {
          "description": "Movement this head controls.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "id",
        "movement"
      ]
    },
    "SignalPhaseSource": {
      "description": "One fixed-time signal phase.",
      "type": "object",
      "properties": {
        "duration_s": {
          "description": "Phase duration in seconds.",
          "type": "number",
          "format": "double"
        },
        "states": {
          "description": "The color of every head in the owning signal during this phase.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/SignalStateSource"
          }
        }
      },
      "additionalProperties": false,
      "required": [
        "duration_s",
        "states"
      ]
    },
    "SignalSource": {
      "description": "A fixed-time signal controller.",
      "type": "object",
      "properties": {
        "heads": {
          "description": "Signal heads controlled together.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/SignalHeadSource"
          }
        },
        "id": {
          "description": "Stable identifier, unique across all authored objects.",
          "type": "string"
        },
        "phases": {
          "description": "Phases in cycle order, each showing every head once.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/SignalPhaseSource"
          }
        }
      },
      "additionalProperties": false,
      "required": [
        "id",
        "heads",
        "phases"
      ]
    },
    "SignalStateSource": {
      "description": "One head's color during one phase.",
      "type": "object",
      "properties": {
        "color": {
          "description": "Color shown on that head during this phase.",
          "$ref": "#/$defs/SignalColor"
        },
        "head": {
          "description": "Head identifier within the owning signal.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "head",
        "color"
      ]
    },
    "SpeedLimitMps": {
      "description": "An enforced speed limit: a finite positive value, or an explicit `null` for\nno enforced limit beyond the mode's own motion limits.\n\nThe value is a required, nullable field, so an absent `limit_mps` is a parse\nerror rather than a silent default; a present `null` is the explicit \"no\nlimit\" form and mirrors the unbounded `TimeIntervalSource.end_s` precedent.\nDeserialization is written by hand so a missing field is rejected: serde's\n`Option`-derived newtype would otherwise treat absence as `null`.",
      "type": [
        "number",
        "null"
      ],
      "format": "double"
    },
    "SpeedPolicySource": {
      "description": "A speed policy: a finite positive limit, or none.",
      "type": "object",
      "properties": {
        "limit_mps": {
          "description": "Enforced limit in metres per second; `null` means no enforced limit.",
          "$ref": "#/$defs/SpeedLimitMps"
        }
      },
      "additionalProperties": false,
      "required": [
        "limit_mps"
      ]
    },
    "TacticKind": {
      "description": "Tactical capability a version-2 mode template supports.",
      "oneOf": [
        {
          "description": "Follow the agent ahead.",
          "type": "string",
          "const": "follow"
        },
        {
          "description": "Stop for a control or an obstruction.",
          "type": "string",
          "const": "stop"
        },
        {
          "description": "Yield to a conflicting movement.",
          "type": "string",
          "const": "yield"
        }
      ]
    },
    "TimeIntervalSource": {
      "description": "The time interval of a version-2 rate demand spawn.",
      "type": "object",
      "properties": {
        "end_s": {
          "description": "Interval end in seconds; `null` is unbounded.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "start_s": {
          "description": "Interval start in seconds.",
          "type": "number",
          "format": "double"
        }
      },
      "additionalProperties": false,
      "required": [
        "start_s"
      ]
    },
    "WaitingAreaSource": {
      "description": "A named waiting area where pedestrians stage between crossings.\n\nThe area is the region itself; the named reference gives a pedestrian route\na stable object to wait at and a later increment a place to attach staging\nbehavior. It traverses no movement and carries no control of its own.",
      "type": "object",
      "properties": {
        "id": {
          "description": "Stable identifier, unique across all authored objects.",
          "type": "string"
        },
        "region": {
          "description": "Traversable region the waiting area occupies.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "id",
        "region"
      ]
    }
  }
}
