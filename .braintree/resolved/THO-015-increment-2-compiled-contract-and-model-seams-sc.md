---
context_rev: 1
updated: 2026-09-14T13:24:50Z
summary: Increment 2 compiled-contract and model seams, schema, and gates.
---

Area [[IDX-001-tangle]].

# Scope

Shared, opt-in reconnaissance for the Increment 2 model and contract surface
(TAS-096, TAS-097, TAS-098 and any child extending the compiled data). Verified
at commit 35d8030.

# Seams

- `crates/tangle-model/src/source.rs`: `ScenarioSourceV2` (~1219),
  `FacilityAdjacencySource` (~923), `TacticKind`, `PermissionKind`,
  `PermissionEffect`, `PermissionSource`, `LateralUse`, `FacilityDirection`.
- `crates/tangle-model/src/compiled.rs` (~4,990 lines):
  `CompiledFacilityAdjacency` (~1494) with `shared_boundary_midpoint()` and
  `shared_boundary_offset(facility, direction)`; `CompiledFacility`,
  `FacilityTraversal`, `UsableLateralInterval`, `CompiledReferencePath`,
  `CompiledModeTemplate::lateral`; `CompiledScenario::traversal_policy`,
  `transitions`, `permission_effect`, `crossing_permission`, `maneuver_policy`,
  `commit_policy`, `wrong_way_policy`, `facility_adjacencies`.
- `crates/tangle-model/src/validate.rs`: `validate_v2` diagnostics,
  `ADJACENCY_TOLERANCE_M = 1e-6`, `shared_boundary_midpoint`,
  `required_profile_params`.

# Contract and schema

- `docs/schema-v2-contract.md` -> *Increment 2 additions*: source shapes,
  compiled semantics, usable/predicted corridors and the four clearance facts,
  transition targets and the geometric handoff, the five-state maneuver
  lifecycle, and the contextual wrong-way decision.
- `schemas/scenario-source.schema.json` is regenerated by the schema-drift test:
  a source-shape change must regenerate it; a compiled-only change must not.

# Gates

`cargo test -p tangle-model`; the schema-drift test; `cargo test --workspace`;
`cargo clippy --workspace --all-targets --all-features -- -D warnings`;
`scripts/check-dependency-direction.sh`.
